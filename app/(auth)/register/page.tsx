import React, { FC } from "react";

type RegisterPageProps = {};

const RegisterPage: FC<RegisterPageProps> = (RegisterPageProps) => {
  return <div>RegisterPage</div>;
};

export default RegisterPage;
