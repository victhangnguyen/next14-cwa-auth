import React, { FC } from "react";

type SettingsPageProps = {};

const SettingsPage: FC<SettingsPageProps> = (SettingsPageProps) => {
  return <div>SettingsPage</div>;
};

export default SettingsPage;
