import React, { FC } from "react";

type DashboardPageProps = {};

const DashboardPage: FC<DashboardPageProps> = (DashboardPageProps) => {
  // const users = await getUsers;
  return <div>DashboardPage</div>;
};

export default DashboardPage;
